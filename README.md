# Team Manager

Brand new project powered by TanStack Start and Vite+.

## Run locally

```bash
vp install
vp dev
```

The dev server runs on `http://localhost:4004` by default.

## Notes

- Use `vp` for all project commands (dev, check, test, build).
- Vite+ docs: https://viteplus.dev/guide/
